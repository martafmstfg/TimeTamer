/* Este componente indica que un obstaculo esta activo, aparece en 
 * pantalla, se esta moviendo y puede chocar contra el jugador */

using Unity.Entities;

public struct ObstaculoActivoComponent : IComponentData {
	
}
