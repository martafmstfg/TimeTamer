/*Componente que, al incluirse en una entidad, hace que se ejecute el
 * sistema SonidoMoneda para reproducir un sonido*/

using Unity.Entities;

public struct SonidoMonedaComponent : IComponentData {
    
}
