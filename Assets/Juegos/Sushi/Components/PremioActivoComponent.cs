/* Este componente indica que un premio esta activo, aparece en 
 * pantalla, se esta moviendo y puede chocar contra el jugador */

using UnityEngine;
using Unity.Entities;

public struct PremioActivoComponent : IComponentData {
	
}
